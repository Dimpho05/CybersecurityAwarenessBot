<Window x:Class="CybersecurityAwarenessBotGUI.ActivityLogWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Activity Log" Height="550" Width="700"
        Background="#0D0D1A" WindowStartupLocation="CenterScreen">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- HEADER -->
        <StackPanel Grid.Row="0" Margin="0,0,0,15">
            <TextBlock Text="[ ACTIVITY LOG ]" FontFamily="Consolas"
                       FontSize="18" FontWeight="Bold" Foreground="#00FFFF"
                       HorizontalAlignment="Center"/>
            <TextBlock x:Name="SubtitleText" Text="Showing last 10 actions"
                       FontFamily="Consolas" FontSize="11"
                       Foreground="#555577" HorizontalAlignment="Center"/>
            <Rectangle Height="1" Fill="#00FFFF" Margin="0,8,0,0" Opacity="0.4"/>
        </StackPanel>

        <!-- LOG LIST -->
        <Border Grid.Row="1" Background="#0A0A14" BorderBrush="#1A1A3E"
                BorderThickness="1" CornerRadius="8" Padding="10">
            <ListBox x:Name="LogListBox" Background="Transparent"
                     BorderThickness="0" Foreground="#00FF99"
                     FontFamily="Consolas" FontSize="12"/>
        </Border>

        <!-- SHOW MORE BUTTON -->
        <Button x:Name="ShowMoreButton" Grid.Row="2"
                Content="▼ SHOW FULL HISTORY"
                Click="ShowMore_Click" Margin="0,10,0,0"
                Background="#1A1A2E" Foreground="#00FFFF"
                FontFamily="Consolas" FontWeight="Bold"
                BorderBrush="#00FFFF" BorderThickness="1"
                Padding="0,8" Cursor="Hand"/>

        <!-- CLOSE BUTTON -->
        <Button Grid.Row="3" Content="CLOSE"
                Click="Close_Click" Margin="0,8,0,0"
                Background="#00FFFF" Foreground="#0D0D1A"
                FontFamily="Consolas" FontWeight="Bold"
                BorderThickness="0" Padding="0,8" Cursor="Hand"/>
    </Grid>
</Window>